
  # High-fidelity web design

  This is a code bundle for High-fidelity web design. The original project is available at https://www.figma.com/design/WXgfWNeTVRObTI4kBOWmue/High-fidelity-web-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  